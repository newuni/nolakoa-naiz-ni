name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  config:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: docker compose config > /dev/null
